with "gnattest.gpr";

project Build is

   for Source_Dirs use (".", "tgen");
   for Object_Dir use "../obj";
   for Exec_Dir use "../bin";
   for Source_Files use ("test-main.adb");

   for Main use ("test-main.adb");

   package Compiler renames GNATtest.Compiler;

   package Builder is
      for Executable ("test-main.adb") use "gnattest";
   end Builder;

   package Binder is
      for Default_Switches ("Ada") use ("-Es");
   end Binder;

   package Linker is
      for Default_Switches ("Ada") use GNATtest.Linker'Switches ("Ada");
   end Linker;

end Build;
