description: |
  This test checks that TGen does not generate wrappers for subprograms
  whose precondition references ghost code.

driver: gnattest_tgen

default-gpr: True

extra_gnattest_args: ["--gen-wrappers"]
