description: Ensure that gnattest generates legal sources when some
  defining names contain trivia tokens, such as newline characters.

driver: python_script
control:
  - [SKIP, 'not native', 'Test not designed for cross applications']
