description: |
  This test check that TGen test case generation still works when a user uses
  GNATCOLL.JSON in its specs.

driver: gnattest_tgen

control:
 - [SKIP, 'not native', 'Test uses GNATCOLL.JSON, not available on cross']
