description:
    Test that TGen is able to generate tests for a discriminated tagged record
    with a non-discriminated type extension.

driver: python_script

control:
 - [SKIP, "rts_light", "tgen not supported on light runtimes"]
