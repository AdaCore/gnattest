description:
    Test that TGen is able to generate tests for tagged types and derived types
    with record extensions.

driver: python_script

control:
 - [SKIP, "rts_light", "tgen not supported on light runtimes"]
