description:
    Test that TGen is able to generate test for an abstract tagged record.
    Currently unsupported.

driver: shell_script

control:
  - [XFAIL, 'x86', 'Marshalling not working for 32bits (UB03-008)']
