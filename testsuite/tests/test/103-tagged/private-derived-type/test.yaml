description:
    Test that TGen is able to generate test for a derived type of a tagged
    record with a private component.

driver: python_script

control:
 - [SKIP, "rts_light", "tgen not supported on light runtimes"]
