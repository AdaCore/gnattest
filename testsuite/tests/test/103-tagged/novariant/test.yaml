description:
    Test that TGen is able to generate tests for discriminated tagged
    record with no variant part.

driver: python_script

control:
 - [SKIP, "rts_light", "tgen not supported on light runtimes"]
