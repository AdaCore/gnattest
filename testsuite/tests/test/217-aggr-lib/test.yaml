description: Check that the stubbing mode of gnattest correctly supports
  aggregate library projects.

driver: python_script
control:
  - [SKIP, 'not native', 'Test not designed for cross applications']
