description: |
  Run TGen test case generator with a complex generic packages architecture.

driver: gnattest_tgen

suppress_test_dump: True

control:
    - [XFAIL, 'True', 'Test loading for generic instances broken, see #80']
