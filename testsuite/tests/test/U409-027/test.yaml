description:
    gnattest test

driver: python_script

# The ada calendar library is not available on some cross target which would fail the test.
control:
  - [SKIP, 'not native', 'Test not designed for cross applications']
