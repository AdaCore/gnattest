description: Test that TGen and gnattest properly support simple proxy
  marshallers. The proxy marshallers should support generation of new values,
  to JSON and binary format, and unmarshalling of those formats to Ada values,
  but not marshalling Ada values to the proxy format.

driver: shell_script
