description: Test that the common switches in the gnatcov package are correctly
  mirrored in the generated coverage_settings.mk file.

driver: python_script

control:
  - [SKIP, 'not native', 'Test not designed for cross applications']
